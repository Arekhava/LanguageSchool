package com.languageschool.entity;

import java.io.Serializable;
import java.util.Collections;
import java.util.List;

/**
 * Describes the entity CourseList
 *
 * @author N
 */
public class CourseList implements Serializable {
	/*
	 * private static final long serialVersionUID = 1L; private List<Course>
	 * courses; private int currentPageNumber; private int numberOfPages;
	 * 
	 *//**
		 * Constructs a new CourseList
		 *//*
			 * public CourseList() { }
			 * 
			 * public List<Course> getCourses() { return
			 * Collections.unmodifiableList(courses); }
			 * 
			 * public void setCourses(List<Course> courses) { this.courses = courses; }
			 * 
			 * public int getCurrentPageNumber() { return currentPageNumber; }
			 * 
			 * public void setCurrentPageNumber(int currentPageNumber) {
			 * this.currentPageNumber = currentPageNumber; }
			 * 
			 * public int getNumberOfPages() { return numberOfPages; }
			 * 
			 * public void setNumberOfPages(int numberOfPages) { this.numberOfPages =
			 * numberOfPages; }
			 * 
			 * @Override public int hashCode() { final int prime = 31; int result = 1;
			 * result = prime * result + currentPageNumber; result = prime * result +
			 * numberOfPages; result = prime * result + ((courses == null) ? 0 :
			 * courses.hashCode()); return result; }
			 * 
			 * @Override public boolean equals(Object obj) { if (this == obj) return true;
			 * if (obj == null) return false; if (getClass() != obj.getClass()) return
			 * false; CourseList other = (CourseList) obj; if (currentPageNumber !=
			 * other.currentPageNumber) return false; if (numberOfPages !=
			 * other.numberOfPages) return false; if (courses == null) { if (other.courses
			 * != null) return false; } else if (!courses.equals(other.courses)) return
			 * false; return true; }
			 * 
			 * @Override public String toString() { StringBuilder builder = new
			 * StringBuilder(); builder.append("CourseList [courses=");
			 * builder.append(courses); builder.append(", currentPageNumber=");
			 * builder.append(currentPageNumber); builder.append(", numberOfPages=");
			 * builder.append(numberOfPages); builder.append("]"); return
			 * builder.toString(); }
			 */
	//TODO
}
